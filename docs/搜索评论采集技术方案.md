# 📋 小红书爬虫技术方案：关键词"51talk老带新"搜索+评论采集

## 一、需求分析

| 需求项 | 说明 |
|--------|------|
| **搜索关键词** | "51talk老带新" |
| **排序方式** | 按时间最近倒排（最新优先） |
| **分页策略** | 分页查询，可控制每页数量和总页数 |
| **评论采集** | 获取每条笔记的所有评论（一级+二级） |
| **数据存储** | Excel + MySQL（便于后续分析） |
| **定时执行** | 每小时自动执行一次 |
| **增量采集** | 与上次数据对比，已存在的评论不再存储 |

---

## 二、技术架构设计

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    系统架构图                                    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   定时调度层    │────▶│   数据采集层    │────▶│   数据对比层    │
│  APScheduler    │     │  小红书API      │     │  增量去重引擎   │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                                                         │
                           ┌─────────────────────────────┘
                           ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   MySQL历史库   │◀───▶│   SQLAlchemy    │     │   增量数据存储  │
│  评论/笔记表    │     │  ORM中间件      │     │  新评论数据     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

### 2.2 时序图

```
┌──────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────┐
│ 定时调度  │    │ 爬虫主程序    │    │ 小红书API    │    │ 数据对比模块  │    │ MySQL    │
└────┬─────┘    └──────┬───────┘    └──────┬───────┘    └──────┬───────┘    └────┬─────┘
     │                 │                   │                   │                │
     │ 触发(每小时)    │                   │                   │                │
     │────────────────▶│                   │                   │                │
     │                 │                   │                   │                │
     │                 │ 1. 加载历史数据   │                   │                │
     │                 │──────────────────────────────────────▶│                │
     │                 │                   │                   │                │
     │                 │ 历史评论ID集合    │                   │                │
     │                 │◀──────────────────────────────────────│                │
     │                 │                   │                   │                │
     │                 │ 2. 搜索笔记       │                   │                │
     │                 │──────────────────▶│                   │                │
     │                 │                   │                   │                │
     │                 │ 返回笔记列表      │                   │                │
     │                 │◀──────────────────│                   │                │
     │                 │                   │                   │                │
     │                 │ 3. 获取笔记详情   │                   │                │
     │                 │──────────────────▶│                   │                │
     │                 │                   │                   │                │
     │                 │ 笔记详情数据      │                   │                │
     │                 │◀──────────────────│                   │                │
     │                 │                   │                   │                │
     │                 │ 4. 获取评论       │                   │                │
     │                 │──────────────────▶│                   │                │
     │                 │                   │                   │                │
     │                 │ 评论数据(原始)    │                   │                │
     │                 │◀──────────────────│                   │                │
     │                 │                   │                   │                │
     │                 │ 5. 评论去重对比   │                   │                │
     │                 │──────────────────────────────────────▶│                │
     │                 │                   │                   │                │
     │                 │                   │ 6. 遍历检查MySQL  │                │
     │                 │                   │                   │────────────────│
     │                 │                   │                   │                │
     │                 │                   │                   │ 返回是否存在   │
     │                 │                   │                   │◀───────────────│
     │                 │                   │                   │                │
     │                 │ 新增评论列表      │                   │                │
     │                 │◀──────────────────────────────────────│                │
     │                 │                   │                   │                │
     │                 │ 7. 保存新增数据   │                   │                │
     │                 │───────────────────────────────────────────────────────▶│
     │                 │                   │                   │                │
     │                 │ 8. 更新历史记录   │                   │                │
     │                 │───────────────────────────────────────────────────────▶│
     │                 │                   │                   │                │
     │ 完成            │                   │                   │                │
     │◀────────────────│                   │                   │                │
     │                 │                   │                   │                │
```

---

## 三、核心模块设计

### 3.1 搜索模块

**API端点**：`POST /api/sns/web/v1/search/notes`

**关键参数**：
```python
{
    "keyword": "51talk老带新",      # 搜索关键词
    "page": 1,                      # 当前页码
    "page_size": 20,                # 每页数量（建议20）
    "sort": "time_descending",      # 排序方式：最新优先
    "note_type": 0,                 # 0不限 1视频 2图文
    "filters": [...]                # 筛选条件
}
```

**排序方式映射**（`sort_type_choice`）：
| 值 | 含义 | API参数 |
|----|------|---------|
| 0 | 综合排序 | `general` |
| 1 | **最新**（选用） | `time_descending` |
| 2 | 最多点赞 | `popularity_descending` |
| 3 | 最多评论 | `comment_descending` |
| 4 | 最多收藏 | `collect_descending` |

### 3.2 评论采集模块

#### 3.2.1 复用现有API

**直接复用 `apis/xhs_pc_apis.py` 中的方法**：

| 方法 | 位置 | 功能 | 说明 |
|------|------|------|------|
| `get_note_all_comment(url, cookies_str, proxies)` | `apis/xhs_pc_apis.py` | 获取笔记所有评论 | ✅ **直接复用，无需重新实现** |

**内部调用链**：
```
get_note_all_comment()  [对外接口]
    │
    ├──▶ get_note_all_out_comment()  [内部方法：分页获取一级评论]
    │       └── 循环调用 /api/sns/web/v2/comment/page
    │
    └──▶ get_note_all_inner_comment()  [内部方法：分页获取二级评论]
            └── 循环调用 /api/sns/web/v2/comment/sub/page
```

#### 3.2.2 复用评论数据处理

**直接复用 `xhs_utils/data_util.py` 中的方法**：

```python
from xhs_utils.data_util import handle_comment_info

# 获取原始评论数据
success, msg, raw_comments = xhs_apis.get_note_all_comment(note_url, cookies_str)

# 复用：标准化处理每条评论
processed_comments = []
for comment in raw_comments:
    comment['note_url'] = note_url  # 补充必要字段
    processed = handle_comment_info(comment)  # ✅ 复用现有处理逻辑
    processed_comments.append(processed)
```

**`handle_comment_info()` 输出数据结构**：
```python
{
    'note_id': '笔记ID',
    'note_url': '笔记链接',
    'comment_id': '评论ID',
    'user_id': '用户ID',
    'home_url': '用户主页链接',
    'nickname': '昵称',
    'avatar': '头像URL',
    'content': '评论内容',
    'show_tags': '评论标签',
    'like_count': '点赞数',
    'upload_time': '发布时间',
    'ip_location': 'IP归属地',
    'pictures': ['图片URL列表'],
}
```

#### 3.2.3 增量采集中的评论处理流程

```python
# search_crawler/crawler_incremental.py

class IncrementalCommentSpider:
    
    def get_all_comments(self, note_url, cookies_str=''):
        """
        获取笔记的所有评论（复用现有API和数据处理）
        """
        from xhs_utils.data_util import handle_comment_info
        
        # Step 1: 复用API获取原始评论数据
        success, msg, raw_comments = self.xhs_apis.get_note_all_comment(
            url=note_url,
            cookies_str=cookies_str
        )
        
        if not success:
            logger.error(f"获取评论失败: {msg}")
            return []
        
        # Step 2: 复用 handle_comment_info() 处理每条评论
        processed_comments = []
        for comment in raw_comments:
            try:
                # 补充 note_url（handle_comment_info需要）
                comment['note_url'] = note_url
                
                # ✅ 复用：调用现有的 handle_comment_info()
                processed = handle_comment_info(comment)
                
                # 补充父评论ID（用于区分一级/二级评论）
                processed['parent_comment_id'] = comment.get('parent_comment_id', '')
                processed['comment_level'] = 2 if processed['parent_comment_id'] else 1
                
                processed_comments.append(processed)
            except Exception as e:
                logger.warning(f"处理评论失败: {e}")
                continue
        
        return processed_comments
```

---

## 四、现有代码复用分析

### 4.1 可复用的现有代码清单

本项目已存在成熟的评论采集和处理逻辑，新功能应直接复用，避免重复实现。

#### 4.1.1 评论采集 API（apis/xhs_pc_apis.py）

| 方法 | 功能 | 复用建议 |
|------|------|---------|
| `get_note_all_comment(url, cookies_str, proxies)` | 获取笔记所有评论（一级+二级） | ✅ **直接复用** |
| `get_note_all_out_comment(note_id, xsec_token, ...)` | 获取所有一级评论（分页） | 内部方法，无需直接调用 |
| `get_note_all_inner_comment(comment, xsec_token, ...)` | 获取所有二级回复（分页） | 内部方法，无需直接调用 |

**关键代码分析**：
```python
# apis/xhs_pc_apis.py - 已存在的评论采集逻辑

def get_note_all_comment(self, url: str, cookies_str: str, proxies: dict = None):
    """
    获取一篇文章的所有评论
    内部实现：
    1. 调用 get_note_all_out_comment() 获取所有一级评论（自动分页）
    2. 遍历每条一级评论，调用 get_note_all_inner_comment() 获取二级回复（自动分页）
    3. 返回完整的评论列表
    """
    out_comment_list = []
    try:
        # 解析URL获取note_id和xsec_token
        urlParse = urllib.parse.urlparse(url)
        note_id = urlParse.path.split("/")[-1]
        kvs = urlParse.query.split('&')
        kvDist = {kv.split('=')[0]: kv.split('=')[1] for kv in kvs}
        
        # 获取所有一级评论
        success, msg, out_comment_list = self.get_note_all_out_comment(
            note_id, kvDist['xsec_token'], cookies_str, proxies
        )
        
        # 获取每条一级评论的二级回复
        for comment in out_comment_list:
            success, msg, new_comment = self.get_note_all_inner_comment(
                comment, kvDist['xsec_token'], cookies_str, proxies
            )
    
    return success, msg, out_comment_list
```

#### 4.1.2 评论数据处理（xhs_utils/data_util.py）

| 函数 | 功能 | 复用建议 |
|------|------|---------|
| `handle_comment_info(data)` | 解析评论原始数据，提取关键字段 | ✅ **直接复用** |
| `save_to_xlsx(datas, file_path, type)` | 保存数据到Excel | ✅ **直接复用** |

**关键代码分析**：
```python
# xhs_utils/data_util.py - 已存在的评论数据处理逻辑

def handle_comment_info(data):
    """
    解析评论原始数据，提取关键字段
    输入：小红书API返回的原始评论数据
    输出：标准化的评论数据结构
    """
    note_id = data['note_id']
    comment_id = data['id']  # API返回的是'id'
    user_id = data['user_info']['user_id']
    nickname = data['user_info']['nickname']
    avatar = data['user_info']['image']
    content = data['content']
    like_count = data['like_count']
    upload_time = timestamp_to_str(data['create_time'])
    ip_location = data.get('ip_location', '未知')
    
    return {
        'note_id': note_id,
        'comment_id': comment_id,
        'user_id': user_id,
        'nickname': nickname,
        'avatar': avatar,
        'content': content,
        'like_count': like_count,
        'upload_time': upload_time,
        'ip_location': ip_location,
    }
```

#### 4.1.3 笔记数据处理（xhs_utils/data_util.py）

| 函数 | 功能 | 复用建议 |
|------|------|---------|
| `handle_note_info(data)` | 解析笔记原始数据，提取关键字段 | ✅ **直接复用** |
| `timestamp_to_str(timestamp)` | 时间戳转字符串 | ✅ **直接复用** |

### 4.2 现有代码调用链分析

```
用户调用
    │
    ▼
┌─────────────────────────┐
│ main.py                 │  Data_Spider.spider_user_all_note(user_url, ...)
│ 业务层入口               │  └── 调用 xhs_apis.get_user_all_notes() 获取笔记列表
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ apis/xhs_pc_apis.py     │  XHS_Apis 类
│ API封装层               │  ├── get_user_all_notes() - 获取用户所有笔记
│                         │  ├── get_note_info() - 获取笔记详情
│                         │  └── get_note_all_comment() - 获取笔记所有评论 ✅ 复用
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ xhs_utils/data_util.py  │  数据处理层
│ 数据处理层               │  ├── handle_note_info() - 处理笔记数据 ✅ 复用
│                         │  ├── handle_comment_info() - 处理评论数据 ✅ 复用
│                         │  └── save_to_xlsx() - 保存到Excel ✅ 复用
└─────────────────────────┘
```

### 4.3 技术方案调整说明

#### 4.3.1 原方案问题

原 `search_crawler/crawler_incremental.py` 实现存在的问题：
1. ❌ 直接操作原始评论数据，未调用 `handle_comment_info()` 进行标准化处理
2. ❌ 评论数据结构可能与 `main.py` 中的不一致
3. ❌ 重复解析评论字段，增加维护成本
4. ❌ **笔记时间未格式化**：`note_upload_time` 直接使用原始时间戳，未转换为可读格式
5. ❌ **邮件字段不完整**：部分字段（如 `show_tags`）未在邮件中显示
6. ❌ **字段路径错误**：搜索API返回的数据结构不同，标题在 `note_card.display_title`，作者在 `note_card.user.nick_name`

#### 4.3.2 调整后方案

**核心原则**：复用现有逻辑，保持数据格式统一

```python
# 调整后的评论采集流程（search_crawler/crawler_incremental.py）

from xhs_utils.data_util import handle_comment_info, handle_note_info, save_to_xlsx, timestamp_to_str

class IncrementalCommentSpider:
    
    def search_notes(self, query, page, sort_type=1, cookies_str=''):
        """搜索笔记"""
        # ... API调用 ...
        
        # ============================================
        # ✅ 修复：使用正确的字段路径
        # 搜索API返回的数据结构：
        # - 笔记标题：note['note_card']['display_title']
        # - 作者昵称：note['note_card']['user']['nick_name'] 或 ['nickname']
        # - 互动数据：note['note_card']['interact_info']['liked_count'] 等
        # ============================================
        notes = [item for item in items if item.get('model_type') == 'note']
        return notes
    
    def get_all_comments(self, note_url, cookies_str=''):
        """
        获取笔记的所有评论（复用现有API）
        """
        try:
            # ✅ 复用：调用 apis/xhs_pc_apis.py 中的 get_note_all_comment()
            success, msg, comments = self.xhs_apis.get_note_all_comment(
                url=note_url,
                cookies_str=cookies_str
            )
            
            if not success:
                logger.error(f"获取评论失败: {msg}")
                return []
            
            # ✅ 复用：调用 handle_comment_info() 标准化评论数据
            processed_comments = []
            for comment in comments:
                try:
                    # 补充 note_url 到评论数据中（handle_comment_info需要）
                    comment['note_url'] = note_url
                    processed_comment = handle_comment_info(comment)
                    processed_comments.append(processed_comment)
                except Exception as e:
                    logger.warning(f"处理评论数据失败: {e}")
                    continue
            
            return processed_comments
            
        except Exception as e:
            logger.error(f"获取评论异常: {e}")
            return []
    
    def run_incremental_crawl(self, ...):
        # ... 采集逻辑 ...
        
        if new_comments:
            # ============================================
            # ✅ 修复：使用正确的字段路径（note_card 下的字段）
            # 笔记数据在 note_card 下，标题是 display_title，用户是 user.nick_name
            # ============================================
            note_card = note.get('note_card', {})
            user_info = note_card.get('user', {})
            
            # 获取笔记标题（display_title 可能为空）
            note_title = note_card.get('display_title', '')
            if not note_title:
                note_title = '无标题'
            
            # 获取作者昵称（nick_name 或 nickname）
            note_author = user_info.get('nick_name') or user_info.get('nickname', '未知作者')
            note_author_id = user_info.get('user_id', '')
            
            # 获取笔记时间（从 corner_tag_info）
            corner_tag = note_card.get('corner_tag_info', [{}])[0] if note_card.get('corner_tag_info') else {}
            note_time_display = corner_tag.get('text', '')  # 如 "5天前"
            
            for comment in new_comments:
                comment['note_id'] = note_id
                comment['note_url'] = note_url
                comment['note_title'] = note_title
                comment['note_author'] = note_author
                comment['note_author_id'] = note_author_id
                comment['note_upload_time'] = note_time_display
```

### 4.4 复用点详细对照表

| 功能点 | 原实现位置 | 新实现方案 | 复用方式 |
|--------|-----------|-----------|---------|
| **一级评论采集** | `search_crawler/crawler_incremental.py` 中重复实现 | `apis/xhs_pc_apis.py:get_note_all_out_comment()` | ✅ 删除重复代码，直接调用 |
| **二级评论采集** | `search_crawler/crawler_incremental.py` 中重复实现 | `apis/xhs_pc_apis.py:get_note_all_inner_comment()` | ✅ 删除重复代码，直接调用 |
| **评论数据解析** | `search_crawler/crawler_incremental.py` 中手动解析 | `xhs_utils/data_util.py:handle_comment_info()` | ✅ 替换为统一处理函数 |
| **笔记数据解析** | `search_crawler/crawler_incremental.py` 中手动解析 | `xhs_utils/data_util.py:handle_note_info()` | ✅ 替换为统一处理函数 |
| **Excel保存** | `search_crawler/crawler_incremental.py` 中手动实现 | `xhs_utils/data_util.py:save_to_xlsx()` + `base_path` | ✅ 直接复用 |
| **时间戳转换** | `search_crawler/crawler_incremental.py` 中重复实现 | `xhs_utils/data_util.py:timestamp_to_str()` | ✅ 直接复用 |
| **搜索API字段映射** | 错误的字段路径 `note['title']`、`note['user']['nickname']` | 正确路径 `note['note_card']['display_title']`、`note['note_card']['user']['nick_name']` | ✅ 修复字段映射 |

---

### 4.5 搜索API数据结构说明

搜索API返回的数据结构与用户主页API不同，需要特别注意字段路径：

#### 4.5.1 搜索API返回数据结构

```python
{
    'id': '笔记ID',
    'model_type': 'note',
    'note_card': {
        'display_title': '笔记标题',  # ✅ 标题在此字段
        'type': 'normal',  # normal=图文, video=视频
        'user': {
            'user_id': '用户ID',
            'nick_name': '用户昵称',  # ✅ 作者昵称在此字段
            'nickname': '用户昵称',   # 备选字段
            'avatar': '头像URL'
        },
        'interact_info': {
            'liked_count': '1',      # 点赞数（字符串）
            'collected_count': '0',  # 收藏数（字符串）
            'comment_count': '4',    # 评论数（字符串）
            'shared_count': '0'      # 分享数（字符串）
        },
        'corner_tag_info': [
            {'text': '5天前', 'type': 'publish_time'}  # 相对时间
        ]
    },
    'xsec_token': 'xxx'
}
```

#### 4.5.2 字段路径对照表

| 字段 | 用户主页API | 搜索API | 修复后代码 |
|------|------------|---------|-----------|
| 笔记标题 | `note['title']` | `note['note_card']['display_title']` | ✅ 已修复 |
| 作者昵称 | `note['user']['nickname']` | `note['note_card']['user']['nick_name']` | ✅ 已修复 |
| 用户ID | `note['user']['user_id']` | `note['note_card']['user']['user_id']` | ✅ 已修复 |
| 点赞数 | `note['likes']` | `note['note_card']['interact_info']['liked_count']` | ✅ 已修复 |
| 评论数 | `note['comments']` | `note['note_card']['interact_info']['comment_count']` | ✅ 已修复 |
| 笔记类型 | `note['type']` | `note['note_card']['type']` | ✅ 已修复 |

### 4.6 Excel保存路径复用方案

#### 4.5.1 现有方案分析

**`main.py` 中的Excel保存方式**：
```python
# main.py
from xhs_utils.common_util import init
from xhs_utils.data_util import save_to_xlsx

# ✅ 复用：调用 init() 获取 base_path
cookies_str, base_path = init()

# 构造完整的Excel文件路径
file_path = os.path.abspath(os.path.join(base_path['excel'], f'{excel_name}.xlsx'))

# ✅ 复用：调用 save_to_xlsx() 保存
save_to_xlsx(note_list, file_path)
```

**`init()` 函数定义**（`xhs_utils/common_util.py`）：
```python
def init():
    """
    初始化函数，返回 cookies_str 和 base_path
    
    base_path 结构：
    {
        'media': '.../datas/media_datas',   # 媒体文件保存路径
        'excel': '.../datas/excel_datas'    # Excel文件保存路径
    }
    """
    media_base_path = os.path.abspath(os.path.join(os.path.dirname(__file__), '../datas/media_datas'))
    excel_base_path = os.path.abspath(os.path.join(os.path.dirname(__file__), '../datas/excel_datas'))
    
    # 自动创建目录（如果不存在）
    for base_path in [media_base_path, excel_base_path]:
        if not os.path.exists(base_path):
            os.makedirs(base_path)
    
    cookies_str = load_env()
    base_path = {
        'media': media_base_path,
        'excel': excel_base_path,
    }
    return cookies_str, base_path
```

#### 4.5.2 调整后方案（复用 base_path）

**核心改动**：
1. `IncrementalCommentSpider.__init__()` 增加 `base_path` 参数
2. 保存Excel时使用 `base_path['excel']` 作为保存路径
3. `main()` 函数调用 `init()` 获取 `base_path` 并传入爬虫类

**修改后的代码**：

```python
# search_crawler/crawler_incremental.py

class IncrementalCommentSpider:
    
    def __init__(self, db_url, base_path=None, email_config=None, ...):
        """
        :param db_url: 数据库连接URL
        :param base_path: 基础路径字典，由 init() 返回
        """
        self.db_manager = DatabaseManager(db_url)
        self.cookie_manager = CookiePoolManager(self.db_manager)
        
        # ============================================
        # ✅ 复用：保存 base_path，用于后续Excel保存
        # ============================================
        self.base_path = base_path
    
    def run_incremental_crawl(self, keyword=None, max_pages=None, cookies_str=''):
        # ... 采集逻辑 ...
        
        # 6. 保存到Excel（可选）
        if all_new_comments:
            # ============================================
            # ✅ 复用：使用 base_path['excel'] 保存Excel
            # 与 main.py 中保存Excel的方式保持一致
            # ============================================
            excel_name = f'{keyword}_{today}_comments'
            file_path = os.path.abspath(os.path.join(self.base_path['excel'], f'{excel_name}.xlsx'))
            
            # ✅ 复用：使用现有的 save_to_xlsx() 保存评论
            save_to_xlsx(all_new_comments, file_path, type='comment')
            logger.info(f"评论已保存到: {file_path}")


def main():
    """主入口"""
    from config import DB_CONFIG, EMAIL_CONFIG
    
    # ============================================
    # ✅ 复用：调用 init() 获取 cookies_str 和 base_path
    # 与 main.py 中的使用方式保持一致
    # ============================================
    cookies_str, base_path = init()
    
    logger.info(f"Excel保存路径: {base_path['excel']}")
    
    # 创建爬虫实例
    spider = IncrementalCommentSpider(
        db_url=DB_CONFIG['url'],
        base_path=base_path,  # ✅ 复用：传入 base_path，用于Excel保存
        email_config=EMAIL_CONFIG if EMAIL_CONFIG.get('sender_email') else None,
        use_mock_email=False
    )
    
    # 执行采集
    notes, comments = spider.run_incremental_crawl(
        keyword=keyword,
        max_pages=max_pages,
        cookies_str=cookies_str
    )
```

#### 4.5.3 路径复用对比

| 项目 | 原实现 | 调整后 | 优点 |
|------|--------|--------|------|
| **路径定义** | `output_dir = 'output'` | `base_path['excel']` | 与 `main.py` 统一 |
| **目录创建** | `os.makedirs(output_dir, exist_ok=True)` | `init()` 自动创建 | 复用已有逻辑 |
| **路径构造** | `f'{output_dir}/{name}.xlsx'` | `os.path.join(base_path['excel'], f'{name}.xlsx')` | 跨平台兼容 |
| **保存调用** | `save_to_xlsx(data, file_path)` | `save_to_xlsx(data, file_path)` | 保持一致 |

**注意**：`base_path` 为必填参数，必须通过 `init()` 函数获取并传入。

#### 4.5.4 目录结构

```
Spider_XHS/
├── datas/                          # 数据保存目录（由 init() 管理）
│   ├── excel_datas/               # Excel文件保存路径
│   │   ├── 51talk老带新_2025-01-15_comments.xlsx
│   │   └── ...
│   └── media_datas/               # 媒体文件保存路径
│       └── ...
├── search_crawler/
│   └── crawler_incremental.py     # 复用 base_path 保存Excel
└── main.py                         # 同样使用 base_path 保存Excel
```

---

## 五、SQLAlchemy ORM 设计

### 5.1 数据库模型定义

```python
# models.py
from sqlalchemy import create_engine, Column, BigInteger, Integer, String, Text, DateTime, TIMESTAMP, UniqueKey, Index
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker
from datetime import datetime

Base = declarative_base()


class CommentHistory(Base):
    """评论历史记录表"""
    __tablename__ = 'comment_history'
    
    id = Column(BigInteger, primary_key=True, autoincrement=True, comment='自增ID')
    unique_id = Column(String(32), unique=True, nullable=False, comment='评论唯一标识(MD5)')
    note_id = Column(String(64), nullable=False, comment='笔记ID', index=True)
    comment_id = Column(String(64), nullable=False, comment='评论ID', index=True)
    parent_comment_id = Column(String(64), nullable=True, comment='父评论ID（一级评论为空）')
    user_id = Column(String(64), nullable=True, comment='用户ID', index=True)
    nickname = Column(String(128), nullable=True, comment='用户昵称')
    content = Column(Text, nullable=True, comment='评论内容')
    like_count = Column(Integer, default=0, comment='点赞数')
    upload_time = Column(DateTime, nullable=True, comment='评论发布时间', index=True)
    ip_location = Column(String(64), nullable=True, comment='IP归属地')
    first_seen_time = Column(TIMESTAMP, default=datetime.now, comment='首次发现时间')
    last_check_time = Column(TIMESTAMP, default=datetime.now, onupdate=datetime.now, comment='最后检查时间')
    
    __table_args__ = (
        {'comment': '评论历史记录表'}
    )


class NoteHistory(Base):
    """笔记历史记录表"""
    __tablename__ = 'note_history'
    
    id = Column(BigInteger, primary_key=True, autoincrement=True, comment='自增ID')
    note_id = Column(String(64), unique=True, nullable=False, comment='笔记ID')
    note_url = Column(String(512), nullable=True, comment='笔记链接')
    title = Column(String(512), nullable=True, comment='笔记标题')
    user_id = Column(String(64), nullable=True, comment='作者用户ID', index=True)
    nickname = Column(String(128), nullable=True, comment='作者昵称')
    note_type = Column(String(32), nullable=True, comment='笔记类型：图集/视频')
    liked_count = Column(Integer, default=0, comment='点赞数')
    collected_count = Column(Integer, default=0, comment='收藏数')
    comment_count = Column(Integer, default=0, comment='评论数')
    share_count = Column(Integer, default=0, comment='分享数')
    upload_time = Column(DateTime, nullable=True, comment='笔记发布时间', index=True)
    first_crawl_time = Column(TIMESTAMP, default=datetime.now, comment='首次采集时间')
    last_crawl_time = Column(TIMESTAMP, default=datetime.now, onupdate=datetime.now, comment='最后采集时间')
    
    __table_args__ = (
        {'comment': '笔记历史记录表'}
    )


class CrawlLog(Base):
    """采集任务日志表"""
    __tablename__ = 'crawl_log'
    
    id = Column(BigInteger, primary_key=True, autoincrement=True, comment='自增ID')
    task_id = Column(String(64), nullable=False, comment='任务ID', index=True)
    keyword = Column(String(256), nullable=True, comment='搜索关键词')
    start_time = Column(TIMESTAMP, default=datetime.now, comment='任务开始时间', index=True)
    end_time = Column(DateTime, nullable=True, comment='任务结束时间')
    status = Column(Integer, default=0, comment='任务状态：0-运行中 1-成功 2-失败')
    total_notes = Column(Integer, default=0, comment='采集笔记总数')
    total_comments = Column(Integer, default=0, comment='采集评论总数')
    new_comments = Column(Integer, default=0, comment='新增评论数')
    error_msg = Column(Text, nullable=True, comment='错误信息')
    
    __table_args__ = (
        {'comment': '采集任务日志表'}
    )


class CookiePool(Base):
    """Cookie池管理表"""
    __tablename__ = 'cookie_pool'
    
    id = Column(Integer, primary_key=True, autoincrement=True, comment='自增ID')
    cookie_name = Column(String(64), unique=True, nullable=False, comment='Cookie标识名称')
    cookie_value = Column(Text, nullable=False, comment='Cookie字符串')
    account_info = Column(String(256), nullable=True, comment='账号信息(备注)')
    status = Column(Integer, default=1, comment='状态：0-失效 1-正常 2-冻结')
    use_count = Column(Integer, default=0, comment='使用次数')
    success_count = Column(Integer, default=0, comment='成功请求次数')
    fail_count = Column(Integer, default=0, comment='失败请求次数')
    last_used_time = Column(DateTime, nullable=True, comment='最后使用时间', index=True)
    last_success_time = Column(DateTime, nullable=True, comment='最后成功时间')
    created_time = Column(TIMESTAMP, default=datetime.now, comment='创建时间')
    
    __table_args__ = (
        {'comment': 'Cookie池管理表'}
    )


class CookieUsageLog(Base):
    """Cookie使用日志表"""
    __tablename__ = 'cookie_usage_log'
    
    id = Column(BigInteger, primary_key=True, autoincrement=True, comment='自增ID')
    cookie_name = Column(String(64), nullable=False, comment='使用的Cookie', index=True)
    task_id = Column(String(64), nullable=True, comment='任务ID', index=True)
    request_url = Column(String(512), nullable=True, comment='请求URL')
    response_status = Column(Integer, nullable=True, comment='响应状态码')
    is_success = Column(Integer, default=0, comment='是否成功：0-失败 1-成功')
    error_msg = Column(Text, nullable=True, comment='错误信息')
    use_time = Column(TIMESTAMP, default=datetime.now, comment='使用时间', index=True)
    
    __table_args__ = (
        {'comment': 'Cookie使用日志表'}
    )


# 数据库连接管理
class DatabaseManager:
    """数据库连接管理器"""
    
    def __init__(self, db_url):
        """
        :param db_url: 数据库连接URL
        格式: mysql+pymysql://user:password@host:port/database?charset=utf8mb4
        """
        self.engine = create_engine(
            db_url,
            pool_size=5,              # 连接池大小
            max_overflow=10,          # 最大溢出连接
            pool_recycle=3600,        # 连接回收时间（秒）
            pool_pre_ping=True,       # 连接前ping测试
            echo=False                # 是否打印SQL语句
        )
        self.Session = sessionmaker(bind=self.engine)
    
    def get_session(self):
        """获取数据库会话"""
        return self.Session()
    
    def close(self):
        """关闭数据库连接"""
        self.engine.dispose()
```

### 6.2 SQLAlchemy 基础操作示例

> **注意**：在使用以下代码前，请先手动执行 [6.3 节的 SQL 初始化脚本](#63-sql-初始化脚本手动执行) 创建数据库表。

```python
# 使用示例
from models import DatabaseManager, CommentHistory, NoteHistory

# 初始化数据库连接（需先手动执行 SQL 脚本建表）
db_manager = DatabaseManager('mysql+pymysql://root:password@localhost/xhs_crawler?charset=utf8mb4')

# 获取会话
session = db_manager.get_session()

try:
    # 查询操作
    exists = session.query(CommentHistory).filter_by(unique_id='xxx').first()
    
    # 批量查询
    unique_ids = ['id1', 'id2', 'id3']
    exists_list = session.query(CommentHistory.unique_id).filter(
        CommentHistory.unique_id.in_(unique_ids)
    ).all()
    
    # 插入操作
    new_comment = CommentHistory(
        unique_id='md5_hash',
        note_id='note_123',
        comment_id='comment_456',
        content='评论内容'
    )
    session.add(new_comment)
    
    # 批量插入
    comments = [CommentHistory(...), CommentHistory(...)]
    session.bulk_save_objects(comments)
    
    # 更新操作
    note = session.query(NoteHistory).filter_by(note_id='xxx').first()
    if note:
        note.comment_count = 100
    
    # 提交事务
    session.commit()
    
except Exception as e:
    session.rollback()
    logger.error(f"数据库操作失败: {e}")
    raise
finally:
    session.close()
```

---

### 6.3 SQL 初始化脚本（手动执行）

如需手动在 MySQL 中创建表，请执行以下 SQL 脚本：

```sql
-- ============================================
-- 小红书爬虫数据库初始化脚本
-- 数据库: xhs_crawler
-- 字符集: utf8mb4
-- ============================================

-- 创建数据库（如不存在）
CREATE DATABASE IF NOT EXISTS xhs_crawler 
    CHARACTER SET utf8mb4 
    COLLATE utf8mb4_unicode_ci;

USE xhs_crawler;

-- ============================================
-- 1. 评论历史记录表
-- ============================================
CREATE TABLE IF NOT EXISTS comment_history (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '自增ID',
    unique_id VARCHAR(32) NOT NULL COMMENT '评论唯一标识(MD5)',
    note_id VARCHAR(64) NOT NULL COMMENT '笔记ID',
    comment_id VARCHAR(64) NOT NULL COMMENT '评论ID',
    parent_comment_id VARCHAR(64) DEFAULT NULL COMMENT '父评论ID（一级评论为空）',
    user_id VARCHAR(64) DEFAULT NULL COMMENT '用户ID',
    nickname VARCHAR(128) DEFAULT NULL COMMENT '用户昵称',
    content TEXT COMMENT '评论内容',
    like_count INT DEFAULT 0 COMMENT '点赞数',
    upload_time DATETIME DEFAULT NULL COMMENT '评论发布时间',
    ip_location VARCHAR(64) DEFAULT NULL COMMENT 'IP归属地',
    first_seen_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '首次发现时间',
    last_check_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后检查时间',
    
    UNIQUE KEY uk_unique_id (unique_id),
    KEY idx_note_id (note_id),
    KEY idx_comment_id (comment_id),
    KEY idx_user_id (user_id),
    KEY idx_upload_time (upload_time),
    KEY idx_first_seen (first_seen_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论历史记录表';

-- ============================================
-- 2. 笔记历史记录表
-- ============================================
CREATE TABLE IF NOT EXISTS note_history (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '自增ID',
    note_id VARCHAR(64) NOT NULL COMMENT '笔记ID',
    note_url VARCHAR(512) DEFAULT NULL COMMENT '笔记链接',
    title VARCHAR(512) DEFAULT NULL COMMENT '笔记标题',
    user_id VARCHAR(64) DEFAULT NULL COMMENT '作者用户ID',
    nickname VARCHAR(128) DEFAULT NULL COMMENT '作者昵称',
    note_type VARCHAR(32) DEFAULT NULL COMMENT '笔记类型：图集/视频',
    liked_count INT DEFAULT 0 COMMENT '点赞数',
    collected_count INT DEFAULT 0 COMMENT '收藏数',
    comment_count INT DEFAULT 0 COMMENT '评论数',
    share_count INT DEFAULT 0 COMMENT '分享数',
    upload_time DATETIME DEFAULT NULL COMMENT '笔记发布时间',
    first_crawl_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '首次采集时间',
    last_crawl_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后采集时间',
    
    UNIQUE KEY uk_note_id (note_id),
    KEY idx_user_id (user_id),
    KEY idx_upload_time (upload_time),
    KEY idx_last_crawl (last_crawl_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='笔记历史记录表';

-- ============================================
-- 3. 采集任务日志表
-- ============================================
CREATE TABLE IF NOT EXISTS crawl_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '自增ID',
    task_id VARCHAR(64) NOT NULL COMMENT '任务ID',
    keyword VARCHAR(256) DEFAULT NULL COMMENT '搜索关键词',
    start_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '任务开始时间',
    end_time TIMESTAMP NULL DEFAULT NULL COMMENT '任务结束时间',
    status TINYINT DEFAULT 0 COMMENT '任务状态：0-运行中 1-成功 2-失败',
    total_notes INT DEFAULT 0 COMMENT '采集笔记总数',
    total_comments INT DEFAULT 0 COMMENT '采集评论总数',
    new_comments INT DEFAULT 0 COMMENT '新增评论数',
    error_msg TEXT COMMENT '错误信息',
    
    KEY idx_task_id (task_id),
    KEY idx_start_time (start_time),
    KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='采集任务日志表';

-- ============================================
-- 4. Cookie池管理表
-- ============================================
CREATE TABLE IF NOT EXISTS cookie_pool (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '自增ID',
    cookie_name VARCHAR(64) NOT NULL COMMENT 'Cookie标识名称',
    cookie_value TEXT NOT NULL COMMENT 'Cookie字符串',
    account_info VARCHAR(256) DEFAULT NULL COMMENT '账号信息(备注)',
    status TINYINT DEFAULT 1 COMMENT '状态：0-失效 1-正常 2-冻结',
    use_count INT DEFAULT 0 COMMENT '使用次数',
    success_count INT DEFAULT 0 COMMENT '成功请求次数',
    fail_count INT DEFAULT 0 COMMENT '失败请求次数',
    last_used_time TIMESTAMP NULL DEFAULT NULL COMMENT '最后使用时间',
    last_success_time TIMESTAMP NULL DEFAULT NULL COMMENT '最后成功时间',
    created_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    
    UNIQUE KEY uk_cookie_name (cookie_name),
    KEY idx_status (status),
    KEY idx_last_used (last_used_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='Cookie池管理表';

-- ============================================
-- 5. Cookie使用日志表
-- ============================================
CREATE TABLE IF NOT EXISTS cookie_usage_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '自增ID',
    cookie_name VARCHAR(64) NOT NULL COMMENT '使用的Cookie',
    task_id VARCHAR(64) DEFAULT NULL COMMENT '任务ID',
    request_url VARCHAR(512) DEFAULT NULL COMMENT '请求URL',
    response_status INT DEFAULT NULL COMMENT '响应状态码',
    is_success TINYINT DEFAULT 0 COMMENT '是否成功：0-失败 1-成功',
    error_msg TEXT COMMENT '错误信息',
    use_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '使用时间',
    
    KEY idx_cookie_name (cookie_name),
    KEY idx_task_id (task_id),
    KEY idx_use_time (use_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='Cookie使用日志表';
```

**执行方式**：

```bash
# 方式1：命令行执行
mysql -u root -p < init.sql

# 方式2：进入MySQL后执行
mysql -u root -p
source /path/to/init.sql;
```

---

## 六、定时任务设计

### 6.1 调度配置

**使用 APScheduler 实现每小时执行**：

```python
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.triggers.cron import CronTrigger

class CommentCrawlerScheduler:
    """评论采集定时调度器"""
    
    def __init__(self, spider):
        self.scheduler = BackgroundScheduler()
        self.spider = spider
        
    def start(self):
        """启动定时任务，每小时执行一次"""
        trigger = CronTrigger(minute='0')
        self.scheduler.add_job(
            self.run_crawl_job,
            trigger=trigger,
            id='comment_crawler_job',
            replace_existing=True
        )
        self.scheduler.start()
        
    def run_crawl_job(self):
        """执行采集任务"""
        try:
            logger.info("=== 定时任务开始执行 ===")
            self.spider.run_incremental_crawl()
            logger.info("=== 定时任务执行完成 ===")
        except Exception as e:
            logger.error(f"定时任务执行失败: {e}")
            
    def shutdown(self):
        """关闭调度器"""
        self.scheduler.shutdown()
```

### 6.2 任务执行流程

```
     每小时触发
         │
         ▼
┌─────────────────┐
│ 检查上次执行状态 │──────────┐
└─────────────────┘          │
         │                   │
         │ 正在执行           │ 空闲
         ▼                   ▼
┌─────────────────┐   ┌─────────────────┐
│ 跳过本次执行    │   │ 加载历史数据    │
└─────────────────┘   └────────┬────────┘
                               │
                               ▼
                        ┌─────────────────┐
                        │ 执行增量采集    │
                        └────────┬────────┘
                               │
                               ▼
                        ┌─────────────────┐
                        │ 是否有新增评论? │
                        └────────┬────────┘
                           是 /   \ 否
                             /     \
                            ▼       ▼
                     ┌────────┐ ┌─────────────┐
                     │保存Excel│ │ 记录无新增  │
                     └────┬───┘ └──────┬──────┘
                         │            │
                         └──────┬─────┘
                                ▼
                        ┌─────────────────┐
                        │ 更新MySQL历史   │
                        └────────┬────────┘
                               │
                               ▼
                        ┌─────────────────┐
                        │ 等待下次触发    │
                        └─────────────────┘
```

---

## 七、增量采集与去重设计

### 8.1 去重策略

**唯一标识生成规则**：
```python
def generate_comment_unique_id(comment):
    """
    生成评论唯一标识
    使用: note_id + comment_id + user_id + content(前50字) + 时间戳
    """
    note_id = comment.get('note_id', '')
    comment_id = comment.get('comment_id', '')
    user_id = comment.get('user_id', '')
    content = comment.get('content', '')[:50]
    upload_time = comment.get('upload_time', '')
    
    unique_str = f"{note_id}_{comment_id}_{user_id}_{content}_{upload_time}"
    import hashlib
    return hashlib.md5(unique_str.encode('utf-8')).hexdigest()
```

### 8.2 增量采集流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    增量采集流程图                                 │
└─────────────────────────────────────────────────────────────────┘

     开始增量采集
         │
         ▼
┌─────────────────┐
│ 读取MySQL历史   │
│ 评论ID库        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 搜索最新笔记    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌──────────────┐
│ 遍历笔记列表    │◀────│ 保存到Excel  │
└────────┬────────┘     └──────┬───────┘
         │                     ▲
         │ 还有笔记            │
         ▼                     │
┌─────────────────┐     ┌──────┴───────┐
│ 获取笔记评论    │────▶│ 批量保存到   │
└────────┬────────┘     │ MySQL        │
         │              └──────┬───────┘
         ▼                     │
┌─────────────────┐            │
│ 生成评论唯一标识│            │
└────────┬────────┘            │
         │                     │
         ▼                     │
┌─────────────────┐            │
│ 检查是否已存在  │────────────┤
└────────┬────────┘            │
    是 /   \ 否               │
      /     \                 │
     ▼       ▼                │
┌────────┐ ┌─────────────┐    │
│ 跳过   │ │ 加入新增列表│────┘
└────────┘ └─────────────┘
                              │
                              ▼
                        ┌─────────────┐
                        │ 还有评论?   │───是──┐
                        └─────────────┘       │
                           否                 │
                            │                 │
                            ▼                 │
                         ┌──────┐             │
                         │ 结束 │◀────────────┘
                         └──────┘
```

### 8.3 核心代码实现（复用现有代码版本）

```python
#!/usr/bin/env python3
# ============================================
# 小红书增量评论采集爬虫 - 复用现有代码版本
# ============================================

import sys
import os
import time
import random
import hashlib
from datetime import datetime

# 添加项目根目录到路径
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from loguru import logger
from sqlalchemy import func

# ============================================
# ✅ 复用 1: 导入现有的API和数据处理工具
# ============================================
from apis.xhs_pc_apis import XHS_Apis
from xhs_utils.data_util import (
    handle_comment_info,    # ✅ 复用：评论数据处理
    handle_note_info,       # ✅ 复用：笔记数据处理  
    save_to_xlsx,           # ✅ 复用：Excel保存
    timestamp_to_str        # ✅ 复用：时间戳转换
)
from xhs_utils.common_util import init

# 导入本包模块
from .config import CRAWL_CONFIG, DELAY_CONFIG
from .models import DatabaseManager, CommentHistory, NoteHistory, CrawlLog
from .cookie_pool_manager import CookiePoolManager
from .email_notifier import EmailNotifier, EmailNotifierMock
from .config import COOKIE_POOL


class IncrementalCommentSpider:
    """
    增量评论采集爬虫 - 复用现有代码版本
    
    本类按照技术方案文档第四章的要求，复用项目中已有的成熟逻辑：
    - 评论采集：复用 apis/xhs_pc_apis.py:get_note_all_comment()
    - 评论处理：复用 xhs_utils/data_util.py:handle_comment_info()
    - Excel保存：复用 xhs_utils/data_util.py:save_to_xlsx() + base_path
    - 时间转换：复用 xhs_utils/data_util.py:timestamp_to_str()
    """
    
    def __init__(self, db_url, base_path=None, email_config=None, use_mock_email=False, auto_sync_cookies=True):
        """
        :param db_url: 数据库连接URL
        :param base_path: 基础路径字典，由 init() 返回，格式为 {'media': ..., 'excel': ...}
        :param email_config: 邮件配置（可选）
        :param use_mock_email: 是否使用邮件模拟器（测试用）
        :param auto_sync_cookies: 是否自动同步 config.py 中的 Cookie 到数据库
        """
        self.db_manager = DatabaseManager(db_url)
        self.cookie_manager = CookiePoolManager(self.db_manager)
        
        # ============================================
        # ✅ 复用：保存 base_path，用于后续Excel保存
        # ============================================
        self.base_path = base_path
        
        # 自动同步 config.py 中的 Cookie 到数据库
        if auto_sync_cookies:
            self._sync_cookies_from_config()
        
        # 初始化邮件通知器
        if email_config:
            if use_mock_email:
                self.email_notifier = EmailNotifierMock(email_config)
            else:
                self.email_notifier = EmailNotifier(email_config)
        else:
            self.email_notifier = None
        
        # ============================================
        # ✅ 复用 2: 初始化小红书API（复用 apis/xhs_pc_apis.py）
        # ============================================
        self.xhs_apis = XHS_Apis()
    
    def _sync_cookies_from_config(self):
        """从 config.py 同步 Cookie 到数据库"""
        for cookie_info in COOKIE_POOL:
            cookie_value = cookie_info.get('value', '').strip()
            if cookie_value:
                self.cookie_manager.add_cookie(
                    cookie_info['name'],
                    cookie_value,
                    cookie_info.get('account_info', '')
                )
    
    # ============================================
    # ✅ 复用 3: 使用 handle_comment_info() 处理评论数据
    # ============================================
    def get_all_comments(self, note_url, cookies_str=''):
        """
        获取笔记的所有评论
        
        复用逻辑：
        1. 调用 apis/xhs_pc_apis.py 中的 get_note_all_comment() 获取原始数据
        2. 调用 xhs_utils/data_util.py 中的 handle_comment_info() 标准化处理
        """
        try:
            # ✅ 复用：调用现有的 get_note_all_comment() API
            success, msg, raw_comments = self.xhs_apis.get_note_all_comment(
                url=note_url,
                cookies_str=cookies_str
            )
            
            if not success:
                logger.error(f"获取评论失败: {msg}")
                return []
            
            # ✅ 复用：使用 handle_comment_info() 处理每条评论
            processed_comments = []
            for comment in raw_comments:
                try:
                    # 补充 note_url（handle_comment_info 需要）
                    comment['note_url'] = note_url
                    
                    # ✅ 复用：调用现有的 handle_comment_info() 进行标准化处理
                    processed = handle_comment_info(comment)
                    
                    # 补充额外字段（用于数据库存储）
                    processed['parent_comment_id'] = comment.get('target_comment_id', '')
                    processed['comment_level'] = 2 if processed['parent_comment_id'] else 1
                    
                    processed_comments.append(processed)
                except Exception as e:
                    logger.warning(f"处理评论数据失败: {e}")
                    continue
            
            return processed_comments
            
        except Exception as e:
            logger.error(f"获取评论异常: {e}")
            return []
    
    def generate_comment_unique_id(self, comment):
        """生成评论唯一标识"""
        note_id = comment.get('note_id', '')
        comment_id = comment.get('comment_id', '')
        user_id = comment.get('user_id', '')
        content = comment.get('content', '')[:50]
        upload_time = comment.get('upload_time', '')
        
        unique_str = f"{note_id}_{comment_id}_{user_id}_{content}_{upload_time}"
        return hashlib.md5(unique_str.encode('utf-8')).hexdigest()
    
    def batch_check_exists(self, session, unique_ids):
        """批量检查评论是否存在，返回已存在的ID集合"""
        if not unique_ids:
            return set()
        
        results = session.query(CommentHistory.unique_id).filter(
            CommentHistory.unique_id.in_(unique_ids)
        ).all()
        return set(row[0] for row in results)
    
    def filter_new_comments(self, session, comments):
        """过滤出新增的评论"""
        for comment in comments:
            comment['_unique_id'] = self.generate_comment_unique_id(comment)
        
        unique_ids = [c['_unique_id'] for c in comments]
        exists_set = self.batch_check_exists(session, unique_ids)
        
        new_comments = [c for c in comments if c['_unique_id'] not in exists_set]
        return new_comments
    
    def save_comment_history_batch(self, session, comments):
        """批量保存评论历史记录"""
        if not comments:
            return 0
        
        comment_objects = []
        for comment in comments:
            ch = CommentHistory(
                unique_id=comment.get('_unique_id'),
                note_id=comment.get('note_id'),
                comment_id=comment.get('comment_id'),
                parent_comment_id=comment.get('parent_comment_id'),
                user_id=comment.get('user_id'),
                nickname=comment.get('nickname'),
                content=comment.get('content'),
                like_count=comment.get('like_count', 0),
                upload_time=comment.get('upload_time'),
                ip_location=comment.get('ip_location')
            )
            comment_objects.append(ch)
        
        session.bulk_save_objects(comment_objects)
        return len(comment_objects)
    
    def save_note_history(self, session, notes):
        """保存笔记历史记录（存在则更新）"""
        for note in notes:
            existing = session.query(NoteHistory).filter_by(
                note_id=note.get('note_id')
            ).first()
            
            if existing:
                # 更新现有记录
                existing.note_url = note.get('note_url')
                existing.title = note.get('title')
                existing.nickname = note.get('nickname')
                existing.liked_count = note.get('liked_count', 0)
                existing.collected_count = note.get('collected_count', 0)
                existing.comment_count = note.get('comment_count', 0)
                existing.share_count = note.get('share_count', 0)
                existing.upload_time = note.get('upload_time')
            else:
                # 插入新记录
                new_note = NoteHistory(
                    note_id=note.get('note_id'),
                    note_url=note.get('note_url'),
                    title=note.get('title'),
                    user_id=note.get('user_id'),
                    nickname=note.get('nickname'),
                    note_type=note.get('note_type'),
                    liked_count=note.get('liked_count', 0),
                    collected_count=note.get('collected_count', 0),
                    comment_count=note.get('comment_count', 0),
                    share_count=note.get('share_count', 0),
                    upload_time=note.get('upload_time')
                )
                session.add(new_note)
    
    def random_delay(self, min_seconds, max_seconds):
        """随机延迟"""
        delay = random.uniform(min_seconds, max_seconds)
        time.sleep(delay)
    
    def search_notes(self, query, page, sort_type=1, cookies_str=''):
        """搜索笔记"""
        try:
            # ✅ 复用：调用现有的 search_note() API
            success, msg, res_json = self.xhs_apis.search_note(
                query=query,
                cookies_str=cookies_str,
                page=page,
                sort_type_choice=sort_type
            )
            
            if not success:
                logger.error(f"搜索笔记失败: {msg}")
                return []
            
            items = res_json.get('data', {}).get('items', [])
            # 过滤只保留笔记类型
            notes = [item for item in items if item.get('model_type') == 'note']
            return notes
            
        except Exception as e:
            logger.error(f"搜索笔记异常: {e}")
            return []
    
    def _get_cookie_for_request(self, cookies_str=''):
        """获取用于请求的 Cookie，优先使用 CookiePoolManager"""
        if cookies_str and len(cookies_str) > 10:
            return cookies_str
        
        cookie_name, cookie_value = self.cookie_manager.get_available_cookie()
        if cookie_value:
            logger.info(f"使用 CookiePool 中的 cookie: {cookie_name}")
            return cookie_value
        
        logger.warning("没有可用的 Cookie，请检查配置")
        return ''
    
    # ============================================
    # ✅ 复用 4: 使用 save_to_xlsx() + base_path 保存Excel
    # ============================================
    def save_increment_data(self, comments, excel_name):
        """
        保存增量数据到Excel
        
        ✅ 复用：使用 base_path['excel'] 作为保存路径，与 main.py 保持一致
        
        评论字段映射（与 save_to_xlsx 中 comment 类型保持一致）：
        - note_id: 笔记id
        - note_url: 笔记url
        - comment_id: 评论id
        - user_id: 用户id
        - home_url: 用户主页url
        - nickname: 昵称
        - avatar: 头像url
        - content: 评论内容
        - show_tags: 评论标签
        - like_count: 点赞数量
        - upload_time: 上传时间（评论发布时间，由 handle_comment_info 格式化）
        - ip_location: ip归属地
        - pictures: 图片地址url列表
        
        :param comments: 评论列表
        :param excel_name: Excel文件名（不含路径和扩展名）
        :return: 保存的文件路径
        """
        if not comments:
            return None
        
        # ============================================
        # ✅ 复用：使用 base_path['excel'] 作为保存路径
        # 与 main.py 中保存Excel的方式保持一致：
        # file_path = os.path.abspath(os.path.join(base_path['excel'], f'{excel_name}.xlsx'))
        # ============================================
        file_path = os.path.abspath(os.path.join(self.base_path['excel'], f'{excel_name}.xlsx'))
        
        # ✅ 复用：使用现有的 save_to_xlsx() 保存评论
        save_to_xlsx(comments, file_path, type='comment')
        logger.info(f"评论已保存到: {file_path}")
        
        return file_path
    
    def run_incremental_crawl(self, keyword=None, max_pages=None, cookies_str=''):
        """
        执行增量采集
        """
        keyword = keyword or CRAWL_CONFIG['keyword']
        max_pages = max_pages or CRAWL_CONFIG['max_pages']
        
        # 获取 Cookie（优先使用传入的，否则从 CookiePool 获取）
        effective_cookies = self._get_cookie_for_request(cookies_str)
        
        task_id = datetime.now().strftime('%Y%m%d_%H%M%S')
        today = datetime.now().strftime('%Y-%m-%d')
        
        session = self.db_manager.get_session()
        
        try:
            # 记录任务开始
            crawl_log = CrawlLog(task_id=task_id, keyword=keyword, status=0)
            session.add(crawl_log)
            session.commit()
            
            logger.info(f"=== 开始增量采集 | 关键词: {keyword} | 任务ID: {task_id} ===")
            
            all_new_comments = []
            all_notes = []
            total_comments = 0
            
            # 1. 分页搜索笔记
            for page in range(1, max_pages + 1):
                logger.info(f"正在搜索第 {page} 页...")
                
                notes = self.search_notes(keyword, page, CRAWL_CONFIG['sort_type'], effective_cookies)
                
                if not notes:
                    logger.info(f"第 {page} 页无数据，停止搜索")
                    break
                
                all_notes.extend(notes)
                logger.info(f"第 {page} 页获取 {len(notes)} 篇笔记")
                
                # 2. 采集每条笔记的评论
                for note in notes:
                    note_id = note.get('id')
                    xsec_token = note.get('xsec_token', '')
                    note_url = f"https://www.xiaohongshu.com/explore/{note_id}?xsec_token={xsec_token}"
                    
                    logger.info(f"正在获取笔记评论: {note_id}")
                    
                    # ✅ 复用：调用 get_all_comments()（内部使用现有的API和数据处理）
                    comments = self.get_all_comments(note_url, effective_cookies)
                    total_comments += len(comments)
                    
                    logger.info(f"笔记 {note_id} 共有 {len(comments)} 条评论")
                    
                    # 3. 过滤新增评论
                    new_comments = self.filter_new_comments(session, comments)
                    
                    if new_comments:
                        # 关联笔记信息
                        for comment in new_comments:
                            comment['note_id'] = note_id
                            comment['note_url'] = note_url
                            comment['note_title'] = note.get('title', '无标题')
                            comment['note_author'] = note.get('user', {}).get('nickname', '未知作者')
                            comment['note_author_id'] = note.get('user', {}).get('user_id', '')
                        
                        all_new_comments.extend(new_comments)
                        
                        # 4. 批量保存到MySQL
                        self.save_comment_history_batch(session, new_comments)
                        session.commit()
                        
                        logger.info(f"笔记 {note_id} 新增 {len(new_comments)} 条评论")
                    
                    # 随机延迟
                    self.random_delay(*DELAY_CONFIG['note_interval'])
                
                # 页间延迟
                self.random_delay(*DELAY_CONFIG['page_interval'])
            
            # 5. 保存笔记历史
            notes_for_save = []
            for note in all_notes:
                notes_for_save.append({
                    'note_id': note.get('id'),
                    'note_url': f"https://www.xiaohongshu.com/explore/{note.get('id')}",
                    'title': note.get('title', '无标题'),
                    'user_id': note.get('user', {}).get('user_id', ''),
                    'nickname': note.get('user', {}).get('nickname', '未知作者'),
                    'note_type': '视频' if note.get('type') == 'video' else '图集',
                    'liked_count': note.get('likes', 0),
                    'collected_count': note.get('collects', 0),
                    'comment_count': note.get('comments', 0),
                    'share_count': note.get('shares', 0),
                    'upload_time': timestamp_to_str(note.get('time')) if note.get('time') else None
                })
            
            self.save_note_history(session, notes_for_save)
            session.commit()
            
            # 6. 保存到Excel（可选）
            if all_new_comments:
                # ============================================
                # ✅ 复用：使用 base_path['excel'] 保存Excel
                # 与 main.py 中保存Excel的方式保持一致
                # ============================================
                excel_name = f'{keyword}_{today}_comments'
                file_path = os.path.abspath(os.path.join(self.base_path['excel'], f'{excel_name}.xlsx'))
                
                # ✅ 复用：使用现有的 save_to_xlsx() 保存评论
                save_to_xlsx(all_new_comments, file_path, type='comment')
                logger.info(f"评论已保存到: {file_path}")
            
            # 7. 发送邮件通知
            if self.email_notifier and all_new_comments:
                unique_users = len(set(c.get('user_id', '') for c in all_new_comments))
                
                crawl_result = {
                    'keyword': keyword,
                    'crawl_time': datetime.now().strftime('%Y-%m-%d %H:%M'),
                    'total_notes': len(all_notes),
                    'new_comments_count': len(all_new_comments),
                    'total_comments': total_comments,
                    'unique_users': unique_users,
                    'success': True,
                    'new_comments_list': all_new_comments
                }
                
                self.email_notifier.send_crawl_report(crawl_result)
            
            # 8. 更新任务日志
            crawl_log.status = 1
            crawl_log.end_time = datetime.now()
            crawl_log.total_notes = len(all_notes)
            crawl_log.total_comments = total_comments
            crawl_log.new_comments = len(all_new_comments)
            session.commit()
            
            logger.info(f"=== 采集完成 | 新增评论: {len(all_new_comments)} 条 ===")
            
            return all_notes, all_new_comments
            
        except Exception as e:
            session.rollback()
            crawl_log.status = 2
            crawl_log.end_time = datetime.now()
            crawl_log.error_msg = str(e)
            session.commit()
            logger.error(f"采集异常: {e}")
            raise
        finally:
            session.close()


def main():
    """主入口"""
    from config import DB_CONFIG, EMAIL_CONFIG
    
    # ============================================
    # ✅ 复用：调用 init() 获取 cookies_str 和 base_path
    # 与 main.py 中的使用方式保持一致
    # ============================================
    cookies_str, base_path = init()
    
    logger.info(f"Excel保存路径: {base_path['excel']}")
    
    # 创建爬虫实例
    spider = IncrementalCommentSpider(
        db_url=DB_CONFIG['url'],
        base_path=base_path,  # ✅ 复用：传入 base_path，用于Excel保存
        email_config=EMAIL_CONFIG if EMAIL_CONFIG.get('sender_email') else None,
        use_mock_email=False
    )
    
    # 执行采集
    keyword = CRAWL_CONFIG['keyword']
    max_pages = CRAWL_CONFIG['max_pages']
    
    logger.info(f"开始采集 | 关键词: {keyword} | 页数: {max_pages}")
    
    try:
        notes, comments = spider.run_incremental_crawl(
            keyword=keyword,
            max_pages=max_pages,
            cookies_str=cookies_str
        )
        logger.info(f"采集完成 | 笔记: {len(notes)} | 新增评论: {len(comments)}")
    except Exception as e:
        logger.error(f"采集失败: {e}")


if __name__ == '__main__':
    main()
```

---

## 八、Cookie池管理方案（SQLAlchemy版本）

### 9.1 Cookie池管理器实现

```python
from datetime import datetime
from sqlalchemy import func, case
from models import DatabaseManager, CookiePool, CookieUsageLog

class CookiePoolManager:
    """Cookie池管理器（SQLAlchemy版本）"""
    
    def __init__(self, db_manager, max_fail_count=5):
        """
        :param db_manager: DatabaseManager实例
        :param max_fail_count: 单Cookie最大失败次数
        """
        self.db_manager = db_manager
        self.max_fail_count = max_fail_count
        self.current_cookie = None
        self.current_cookie_name = None
        
    def add_cookie(self, cookie_name, cookie_value, account_info=''):
        """添加新Cookie到池中"""
        session = self.db_manager.get_session()
        try:
            # 查询是否已存在
            existing = session.query(CookiePool).filter_by(cookie_name=cookie_name).first()
            
            if existing:
                # 更新现有Cookie
                existing.cookie_value = cookie_value
                existing.account_info = account_info
                existing.status = 1
                existing.fail_count = 0
            else:
                # 创建新Cookie记录
                new_cookie = CookiePool(
                    cookie_name=cookie_name,
                    cookie_value=cookie_value,
                    account_info=account_info,
                    status=1
                )
                session.add(new_cookie)
            
            session.commit()
            logger.info(f"Cookie [{cookie_name}] 已添加到池中")
        except Exception as e:
            session.rollback()
            logger.error(f"添加Cookie失败: {e}")
        finally:
            session.close()
    
    def get_available_cookie(self, strategy='round_robin'):
        """
        获取可用的Cookie
        :param strategy: 选择策略 round_robin/random/least_used
        """
        session = self.db_manager.get_session()
        try:
            query = session.query(CookiePool).filter_by(status=1)
            
            if strategy == 'round_robin':
                # 按最后使用时间排序，最早使用的优先
                query = query.order_by(CookiePool.last_used_time.asc().nulls_first())
            elif strategy == 'random':
                # 随机排序
                query = query.order_by(func.rand())
            elif strategy == 'least_used':
                # 使用次数最少的优先
                query = query.order_by(CookiePool.use_count.asc())
            
            result = query.first()
            
            if result:
                self.current_cookie_name = result.cookie_name
                self.current_cookie = result.cookie_value
                
                # 更新使用时间和次数
                result.last_used_time = datetime.now()
                result.use_count += 1
                session.commit()
                
                return result.cookie_name, result.cookie_value
            else:
                logger.error("没有可用的Cookie，请检查Cookie池")
                return None, None
        finally:
            session.close()
    
    def report_cookie_result(self, cookie_name, is_success, error_msg=None):
        """报告Cookie使用结果"""
        session = self.db_manager.get_session()
        try:
            cookie = session.query(CookiePool).filter_by(cookie_name=cookie_name).first()
            
            if cookie:
                if is_success:
                    cookie.success_count += 1
                    cookie.last_success_time = datetime.now()
                else:
                    cookie.fail_count += 1
                    
                    # 检查是否超过最大失败次数
                    if cookie.fail_count >= self.max_fail_count:
                        cookie.status = 0
                        logger.warning(f"Cookie [{cookie_name}] 连续失败{self.max_fail_count}次，已标记为失效")
                
                session.commit()
            
            # 记录使用日志
            usage_log = CookieUsageLog(
                cookie_name=cookie_name,
                is_success=1 if is_success else 0,
                error_msg=error_msg
            )
            session.add(usage_log)
            session.commit()
            
        except Exception as e:
            session.rollback()
            logger.error(f"报告Cookie结果失败: {e}")
        finally:
            session.close()
    
    def get_pool_status(self):
        """获取Cookie池状态统计"""
        session = self.db_manager.get_session()
        try:
            results = session.query(
                CookiePool.status,
                func.count().label('count'),
                func.sum(CookiePool.use_count).label('total_use'),
                func.sum(CookiePool.success_count).label('total_success'),
                func.sum(CookiePool.fail_count).label('total_fail')
            ).group_by(CookiePool.status).all()
            
            return results
        finally:
            session.close()
    
    def print_pool_status(self):
        """打印Cookie池状态"""
        status = self.get_pool_status()
        
        print("=" * 60)
        print("Cookie池状态")
        print("=" * 60)
        
        status_map = {0: '失效', 1: '正常', 2: '冻结'}
        
        for item in status:
            status_name = status_map.get(item.status, '未知')
            print(f"状态 [{status_name}]: {item.count} 个")
            print(f"  - 总使用次数: {item.total_use or 0}")
            print(f"  - 成功: {item.total_success or 0} | 失败: {item.total_fail or 0}")
            
            if item.total_use and item.total_use > 0:
                success_rate = (item.total_success or 0) / item.total_use * 100
                print(f"  - 成功率: {success_rate:.1f}%")
        print("=" * 60)
```

### 9.2 Cookie池装饰器

```python
class CookiePoolDecorator:
    """Cookie池装饰器 - 为API请求自动管理Cookie"""
    
    def __init__(self, cookie_pool_manager):
        self.cookie_manager = cookie_pool_manager
    
    def __call__(self, func):
        def wrapper(*args, **kwargs):
            max_retry = 3
            for i in range(max_retry):
                # 获取可用Cookie
                cookie_name, cookie = self.cookie_manager.get_available_cookie()
                if not cookie:
                    raise Exception("没有可用的Cookie")
                
                try:
                    # 执行请求
                    result = func(*args, cookies_str=cookie, **kwargs)
                    
                    # 检查结果是否成功
                    if isinstance(result, tuple) and len(result) >= 2:
                        success = result[0]
                        msg = result[1] if len(result) > 1 else ''
                        
                        # 检查是否Cookie失效的特征
                        if not success and self._is_cookie_invalid(msg):
                            self.cookie_manager.report_cookie_result(cookie_name, False, msg)
                            logger.warning(f"Cookie失效，自动切换 [{cookie_name}] -> 下一个")
                            continue
                        else:
                            self.cookie_manager.report_cookie_result(cookie_name, success)
                            return result
                    else:
                        self.cookie_manager.report_cookie_result(cookie_name, True)
                        return result
                        
                except Exception as e:
                    error_msg = str(e)
                    self.cookie_manager.report_cookie_result(cookie_name, False, error_msg)
                    
                    # 判断是否需要切换Cookie重试
                    if self._is_cookie_invalid(error_msg) and i < max_retry - 1:
                        logger.warning(f"请求异常，切换Cookie重试 [{i+1}/{max_retry}]")
                        continue
                    else:
                        raise
            
            raise Exception(f"重试{max_retry}次后仍然失败")
        
        return wrapper
    
    def _is_cookie_invalid(self, error_msg):
        """判断错误是否由Cookie失效导致"""
        invalid_keywords = [
            '登录', 'cookie', 'session', '失效', '过期',
            '未登录', '鉴权', 'auth', 'forbidden', 'unauthorized'
        ]
        error_lower = str(error_msg).lower()
        return any(keyword in error_lower for keyword in invalid_keywords)
```

---

## 九、邮件通知方案

### 10.1 功能概述

每次增量采集完成后，系统将自动发送邮件到指定QQ邮箱，通知本次采集结果：
- **触发时机**：Excel保存成功后
- **邮件内容**：新增评论统计、完整评论详情（所有新增评论）
- **接收方式**：QQ邮箱（支持其他SMTP邮箱）

### 10.2 邮件内容设计

#### 邮件主题
```
【小红书爬虫】51talk老带新 - 2025-01-15 14:00 采集报告 | 新增评论23条
```

#### 邮件正文（HTML格式）

```html
<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .header { background: #ff2442; color: white; padding: 20px; border-radius: 5px; }
        .stats { background: #f5f5f5; padding: 15px; margin: 15px 0; border-left: 4px solid #ff2442; }
        .comment-item { border-bottom: 1px solid #eee; padding: 10px 0; }
        .nickname { color: #ff2442; font-weight: bold; }
        .time { color: #999; font-size: 12px; }
        .footer { margin-top: 20px; padding-top: 20px; border-top: 1px solid #eee; color: #666; font-size: 12px; }
    </style>
</head>
<body>
    <div class="header">
        <h2>🕷️ 小红书爬虫采集报告</h2>
        <p>关键词：51talk老带新 | 采集时间：2025-01-15 14:00</p>
    </div>
    
    <div class="stats">
        <h3>📊 采集统计</h3>
        <ul>
            <li>采集笔记数：<strong>15</strong> 篇</li>
            <li>新增评论数：<strong>23</strong> 条</li>
            <li>涉及用户数：<strong>18</strong> 人</li>
            <li>任务状态：<span style="color: green;">✅ 成功</span></li>
        </ul>
    </div>
    
    <h3>💬 新增评论详情（共 {new_comments_count} 条）</h3>
    <div class="comment-list">
        <!-- 笔记1的评论组 -->
        <div class="note-group" style="margin: 20px 0; border: 1px solid #ffe4e4; border-radius: 8px; overflow: hidden;">
            <!-- 笔记信息（含作者） -->
            <div style="background: linear-gradient(135deg, #ff2442, #ff5c7c); color: white; padding: 12px 15px;">
                <div style="font-size: 16px; font-weight: bold;">📄 51talk老带新活动攻略</div>
                <div style="font-size: 13px; margin-top: 6px; opacity: 0.95;">
                    <span>👤 作者：@小红书用户123</span>
                    <span style="margin-left: 20px;">🕐 笔记时间：2025-01-14</span>
                    <a href="{note_url}" style="color: #fff; text-decoration: underline; margin-left: 20px;">查看笔记 →</a>
                </div>
            </div>
            <!-- 该笔记下的评论 -->
            <div style="padding: 15px; background: #fff;">
                <div style="border-bottom: 1px dashed #eee; padding: 12px 0;">
                    <div style="display: flex; align-items: center; flex-wrap: wrap; gap: 10px;">
                        <span style="color: #ff2442; font-weight: bold; font-size: 14px;">👤 @评论用户A</span>
                        <span style="color: #666; font-size: 12px;">🕐 2025-01-15 13:30</span>
                        <span style="color: #999; font-size: 12px;">📍 北京</span>
                        <span style="color: #ff2442; font-size: 12px;">❤️ 5</span>
                    </div>
                    <p style="margin: 8px 0 0 0; padding: 10px; background: #f9f9f9; border-radius: 4px;">
                        我也参加过51talk的老带新活动，确实挺划算的...
                    </p>
                </div>
                <div style="padding: 12px 0;">
                    <div style="display: flex; align-items: center; flex-wrap: wrap; gap: 10px;">
                        <span style="color: #ff2442; font-weight: bold; font-size: 14px;">👤 @评论用户B</span>
                        <span style="color: #666; font-size: 12px;">🕐 2025-01-15 12:15</span>
                        <span style="color: #999; font-size: 12px;">📍 上海</span>
                        <span style="color: #ff2442; font-size: 12px;">❤️ 3</span>
                    </div>
                    <p style="margin: 8px 0 0 0; padding: 10px; background: #f9f9f9; border-radius: 4px;">
                        新人报名有什么优惠吗？求推荐码
                    </p>
                </div>
            </div>
        </div>
        
        <!-- 笔记2的评论组 -->
        <div class="note-group" style="margin: 20px 0; border: 1px solid #ffe4e4; border-radius: 8px; overflow: hidden;">
            <div style="background: linear-gradient(135deg, #ff2442, #ff5c7c); color: white; padding: 12px 15px;">
                <div style="font-size: 16px; font-weight: bold;">📄 51talk学英语真实体验分享</div>
                <div style="font-size: 13px; margin-top: 6px; opacity: 0.95;">
                    <span>👤 作者：@英语达人小王</span>
                    <span style="margin-left: 20px;">🕐 笔记时间：2025-01-13</span>
                    <a href="{note_url}" style="color: #fff; text-decoration: underline; margin-left: 20px;">查看笔记 →</a>
                </div>
            </div>
            <div style="padding: 15px; background: #fff;">
                <div style="padding: 12px 0;">
                    <div style="display: flex; align-items: center; flex-wrap: wrap; gap: 10px;">
                        <span style="color: #ff2442; font-weight: bold; font-size: 14px;">👤 @评论用户C</span>
                        <span style="color: #666; font-size: 12px;">🕐 2025-01-15 11:20</span>
                        <span style="color: #999; font-size: 12px;">📍 广州</span>
                        <span style="color: #ff2442; font-size: 12px;">❤️ 8</span>
                    </div>
                    <p style="margin: 8px 0 0 0; padding: 10px; background: #f9f9f9; border-radius: 4px;">
                        已经学了三个月了，进步很大！
                    </p>
                </div>
            </div>
        </div>
    </div>
    
    <div class="footer">
        <p>此邮件由小红书爬虫系统自动发送</p>
    </div>
</body>
</html>
```

### 10.3 SMTP 邮件发送实现

```python
# email_notifier.py
import smtplib
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart
from email.mime.base import MIMEBase
from email import encoders
from email.utils import formatdate
from datetime import datetime
import os


class EmailNotifier:
    """邮件通知器 - 支持QQ邮箱/企业邮箱等"""
    
    def __init__(self, smtp_config):
        """
        :param smtp_config: SMTP配置字典
        {
            'smtp_server': 'smtp.qq.com',      # SMTP服务器
            'smtp_port': 465,                   # 端口（QQ邮箱用465/587）
            'sender_email': 'your_qq@qq.com',   # 发件人邮箱
            'sender_password': 'authorization_code',  # 授权码（非登录密码）
            'receiver_email': 'target@qq.com'   # 收件人邮箱
        }
        """
        self.smtp_config = smtp_config
        
    def send_crawl_report(self, crawl_result):
        """
        发送采集报告邮件
        :param crawl_result: 采集结果字典
        """
        try:
            # 构建邮件
            msg = MIMEMultipart()
            msg['From'] = self.smtp_config['sender_email']
            msg['To'] = self.smtp_config['receiver_email']
            msg['Date'] = formatdate(localtime=True)
            msg['Subject'] = self._build_subject(crawl_result)
            
            # 添加HTML正文
            html_body = self._build_html_body(crawl_result)
            msg.attach(MIMEText(html_body, 'html', 'utf-8'))
            
            # 连接SMTP服务器并发送
            server = smtplib.SMTP_SSL(
                self.smtp_config['smtp_server'], 
                self.smtp_config['smtp_port']
            )
            server.login(
                self.smtp_config['sender_email'],
                self.smtp_config['sender_password']
            )
            server.sendmail(
                self.smtp_config['sender_email'],
                self.smtp_config['receiver_email'],
                msg.as_string()
            )
            server.quit()
            
            logger.info(f"邮件发送成功: {self.smtp_config['receiver_email']}")
            return True
            
        except Exception as e:
            logger.error(f"邮件发送失败: {e}")
            return False
    
    def _build_subject(self, result):
        """构建邮件主题"""
        keyword = result.get('keyword', '51talk老带新')
        crawl_time = result.get('crawl_time', datetime.now().strftime('%Y-%m-%d %H:%M'))
        new_count = result.get('new_comments_count', 0)
        status = '成功' if result.get('success') else '失败'
        
        return f"【小红书爬虫】{keyword} - {crawl_time} 采集报告 | 新增评论{new_count}条 | {status}"
    
    def _build_html_body(self, result):
        """构建HTML邮件正文"""
        keyword = result.get('keyword', '51talk老带新')
        crawl_time = result.get('crawl_time', datetime.now().strftime('%Y-%m-%d %H:%M'))
        total_notes = result.get('total_notes', 0)
        new_comments = result.get('new_comments_count', 0)
        total_comments = result.get('total_comments', 0)
        unique_users = result.get('unique_users', 0)
        status = result.get('success', True)
        comments_list = result.get('new_comments_list', [])
        
        status_html = '<span style="color: green;">✅ 成功</span>' if status else '<span style="color: red;">❌ 失败</span>'
        
        # 按笔记ID分组构建评论列表HTML（显示所有评论）
        from collections import defaultdict
        
        # 按笔记分组
        comments_by_note = defaultdict(list)
        for comment in comments_list:
            note_id = comment.get('note_id', 'unknown')
            comments_by_note[note_id].append(comment)
        
        comments_html = ''
        note_index = 1
        
        for note_id, note_comments in comments_by_note.items():
            if not note_comments:
                continue
            
            # 获取笔记信息（使用第一条评论的笔记信息）
            first_comment = note_comments[0]
            note_url = first_comment.get('note_url', f'https://www.xiaohongshu.com/explore/{note_id}')
            note_title = first_comment.get('note_title', f'笔记 {note_id}')
            note_author = first_comment.get('note_author', '未知作者')  # 笔记作者昵称
            note_author_id = first_comment.get('note_author_id', '')   # 笔记作者ID
            note_time = first_comment.get('note_upload_time', '')      # 笔记发布时间
            
            # 笔记信息区域（含作者信息）
            comments_html += f"""
            <div style="margin: 20px 0; border: 1px solid #ffe4e4; border-radius: 8px; overflow: hidden;">
                <div style="background: linear-gradient(135deg, #ff2442, #ff5c7c); color: white; padding: 12px 15px;">
                    <div style="font-size: 16px; font-weight: bold;">📄 {note_title[:60]}{'...' if len(note_title) > 60 else ''}</div>
                    <div style="font-size: 13px; margin-top: 6px; opacity: 0.95;">
                        <span>👤 作者：@{note_author}</span>
                        {f'<span style="margin-left: 20px;">🕐 笔记时间：{note_time}</span>' if note_time else ''}
                        <a href="{note_url}" style="color: #fff; text-decoration: underline; margin-left: 20px;">查看笔记 →</a>
                    </div>
                </div>
                <div style="padding: 15px; background: #fff;">
            """
            
            # 该笔记下的所有评论
            for i, comment in enumerate(note_comments, 1):
                # ============================================
                # ✅ 字段映射：与 save_to_xlsx 中 comment 类型字段保持一致
                # save_to_xlsx headers: ['笔记id', '笔记url', '评论id', '用户id', '用户主页url', '昵称', '头像url', 
                #                       '评论内容', '评论标签', '点赞数量', '上传时间', 'ip归属地', '图片地址url列表']
                # 邮件中显示的字段（排除：笔记id, 评论id, 用户id, 图片地址url列表）
                # ============================================
                nickname = comment.get('nickname', '未知用户')
                upload_time = comment.get('upload_time', '')  # 评论发布时间
                ip_location = comment.get('ip_location', '未知')
                content = comment.get('content', '')
                like_count = comment.get('like_count', 0)
                show_tags = comment.get('show_tags', '')  # 评论标签
                home_url = comment.get('home_url', '')  # 用户主页url
                avatar = comment.get('avatar', '')  # 头像url
                
                # 构建评论标签显示
                tags_html = f'<span style="color: #999; font-size: 12px;">🏷️ {show_tags}</span>' if show_tags else ''
                
                # 构建头像显示（如果有）
                avatar_html = f'<img src="{avatar}" style="width: 32px; height: 32px; border-radius: 50%; margin-right: 8px;" />' if avatar else ''
                
                # 构建昵称链接（如果有用户主页url）
                if home_url:
                    nickname_html = f'<a href="{home_url}" style="color: #ff2442; font-weight: bold; font-size: 14px; text-decoration: none;">👤 @{nickname}</a>'
                else:
                    nickname_html = f'<span style="color: #ff2442; font-weight: bold; font-size: 14px;">👤 @{nickname}</span>'
                
                comments_html += f"""
                    <div style="border-bottom: {'1px dashed #eee' if i < len(note_comments) else 'none'}; padding: 12px 0;">
                        <div style="display: flex; align-items: center; flex-wrap: wrap; gap: 10px; margin-bottom: 8px;">
                            {avatar_html}
                            {nickname_html}
                            <span style="color: #666; font-size: 12px;">🕐 {upload_time}</span>
                            <span style="color: #999; font-size: 12px;">📍 {ip_location}</span>
                            <span style="color: #ff2442; font-size: 12px;">❤️ {like_count}</span>
                            {tags_html}
                        </div>
                        <p style="margin: 0; padding: 10px; background: #f9f9f9; border-radius: 4px; line-height: 1.6;">
                            {content}
                        </p>
                    </div>
                """
            
            comments_html += f"""
                    <div style="margin-top: 10px; padding-top: 10px; border-top: 1px solid #f0f0f0; color: #666; font-size: 12px;">
                        该笔记共 <strong>{len(note_comments)}</strong> 条新增评论
                    </div>
                </div>
            </div>
            """
            note_index += 1
        
        html = f"""
        <!DOCTYPE html>
        <html>
        <head>
            <meta charset="UTF-8">
            <style>
                body {{ font-family: Arial, sans-serif; line-height: 1.6; color: #333; max-width: 800px; margin: 0 auto; }}
                .header {{ background: linear-gradient(135deg, #ff2442, #ff5c7c); color: white; padding: 20px; border-radius: 8px; margin-bottom: 20px; }}
                .stats {{ background: #f8f9fa; padding: 20px; margin: 15px 0; border-left: 4px solid #ff2442; border-radius: 4px; }}
                .stats ul {{ list-style: none; padding: 0; margin: 0; }}
                .stats li {{ padding: 8px 0; border-bottom: 1px dashed #ddd; }}
                .stats li:last-child {{ border-bottom: none; }}
                .stats strong {{ color: #ff2442; font-size: 18px; }}
                .comment-list {{ margin: 20px 0; }}
                .comment-item {{ margin: 15px 0; }}
                .note-header {{ background: linear-gradient(135deg, #ff2442, #ff5c7c); color: white; padding: 12px 15px; border-radius: 8px 8px 0 0; }}
                .comment-meta {{ display: flex; align-items: center; flex-wrap: wrap; gap: 10px; }}
                .footer {{ margin-top: 30px; padding-top: 20px; border-top: 2px solid #eee; color: #666; font-size: 12px; text-align: center; }}
            </style>
        </head>
        <body>
            <div class="header">
                <h2>🕷️ 小红书爬虫采集报告</h2>
                <p>关键词：<strong>{keyword}</strong> | 采集时间：{crawl_time}</p>
            </div>
            
            <div class="stats">
                <h3>📊 采集统计</h3>
                <ul>
                    <li>📄 采集笔记数：<strong>{total_notes}</strong> 篇</li>
                    <li>💬 新增评论数：<strong>{new_comments}</strong> 条（共采集 {total_comments} 条）</li>
                    <li>👤 涉及用户数：<strong>{unique_users}</strong> 人</li>
                    <li>📌 任务状态：{status_html}</li>
                </ul>
            </div>
            
            <h3>💬 新增评论详情（共 {new_comments} 条）</h3>
            <div class="comment-list">
                {comments_html}
            </div>
            
            <div class="footer">
                <p>此邮件由小红书爬虫系统自动发送 | 发送时间：{datetime.now().strftime('%Y-%m-%d %H:%M:%S')}</p>
            </div>
        </body>
        </html>
        """
        return html


class EmailNotifierMock:
    """邮件通知模拟器（测试用，不实际发送邮件）"""
    
    def __init__(self, smtp_config):
        self.smtp_config = smtp_config
    
    def send_crawl_report(self, crawl_result):
        """模拟发送，仅记录日志"""
        logger.info("[MOCK] 邮件发送模拟")
        logger.info(f"[MOCK] 收件人: {self.smtp_config['receiver_email']}")
        logger.info(f"[MOCK] 主题: {crawl_result.get('keyword', '')}")
        logger.info(f"[MOCK] 新增评论数: {crawl_result.get('new_comments_count', 0)}")
        return True
```

### 10.4 集成到采集流程

```python
# crawler_incremental.py 集成邮件通知

class IncrementalCommentSpider:
    def __init__(self, db_url, email_config=None):
        # 注意：需先手动执行 SQL 初始化脚本创建表
        self.db_manager = DatabaseManager(db_url)
        
        # 初始化邮件通知器
        if email_config:
            self.email_notifier = EmailNotifier(email_config)
        else:
            self.email_notifier = None
    
    def run_incremental_crawl(self, keyword="51talk老带新", max_pages=5):
        """执行增量采集（集成邮件通知）"""
        task_id = datetime.now().strftime('%Y%m%d_%H%M%S')
        today = datetime.now().strftime('%Y-%m-%d')
        
        # ... 采集逻辑 ...
        
        # 6. 发送邮件通知（如有新增评论）
        if self.email_notifier and all_new_comments:
            # 构建邮件报告数据
            unique_users = len(set(c['user_id'] for c in all_new_comments))
            
            # 为每条评论补充笔记信息（标题、作者等）
            note_info_map = {
                n['id']: {
                    'title': n.get('title', '无标题'),
                    'author': n.get('nickname', '未知作者'),
                    'author_id': n.get('user_id', ''),
                    'upload_time': n.get('upload_time', '')
                } for n in all_notes
            }
            for comment in all_new_comments:
                note_info = note_info_map.get(comment['note_id'], {})
                comment['note_title'] = note_info.get('title', '无标题')
                comment['note_author'] = note_info.get('author', '未知作者')
                comment['note_author_id'] = note_info.get('author_id', '')
                comment['note_upload_time'] = note_info.get('upload_time', '')
            
            crawl_result = {
                'keyword': keyword,
                'crawl_time': datetime.now().strftime('%Y-%m-%d %H:%M'),
                'total_notes': len(all_notes),
                'new_comments_count': len(all_new_comments),
                'total_comments': total_comments,
                'unique_users': unique_users,
                'success': True,
                'new_comments_list': all_new_comments  # 传递所有评论，邮件中展示全部
            }
            
            # 发送邮件
            self.email_notifier.send_crawl_report(crawl_result)
        
        return all_notes, all_new_comments
```

### 10.5 QQ邮箱配置指南

#### 8.5.1 获取授权码

1. 登录 QQ 邮箱网页版
2. 点击顶部【设置】→【账户】
3. 找到【POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务】
4. 开启【IMAP/SMTP服务】
5. 按提示发送短信验证，获取 **16位授权码**（非登录密码）

#### 8.5.2 配置示例

```python
# config.py

EMAIL_CONFIG = {
    # QQ邮箱配置
    'smtp_server': 'smtp.qq.com',
    'smtp_port': 465,  # SSL端口
    'sender_email': 'your_qq_number@qq.com',  # 你的QQ邮箱
    'sender_password': 'abcdefghijklmnop',     # 16位授权码
    'receiver_email': 'target_qq@qq.com'       # 收件人QQ邮箱
}

# 企业邮箱配置示例（如有需要）
EMAIL_CONFIG_ENT = {
    'smtp_server': 'smtp.exmail.qq.com',
    'smtp_port': 465,
    'sender_email': 'notice@yourcompany.com',
    'sender_password': 'your_password',
    'receiver_email': 'manager@yourcompany.com'
}
```

#### 8.5.3 常用邮箱SMTP配置

| 邮箱类型 | SMTP服务器 | 端口 | SSL |
|---------|-----------|------|-----|
| QQ邮箱 | smtp.qq.com | 465/587 | 是 |
| 163邮箱 | smtp.163.com | 465/994 | 是 |
| Gmail | smtp.gmail.com | 465/587 | 是 |
| Outlook | smtp.office365.com | 587 | 是 |
| 企业微信邮箱 | smtp.exmail.qq.com | 465 | 是 |

### 10.6 邮件发送失败处理

```python
def send_email_with_retry(self, crawl_result, max_retry=3):
    """带重试机制的邮件发送"""
    for i in range(max_retry):
        try:
            success = self.send_crawl_report(crawl_result)
            if success:
                return True
        except Exception as e:
            logger.error(f"邮件发送失败（第{i+1}次）: {e}")
            time.sleep(5)  # 等待5秒后重试
    
    logger.error(f"邮件发送失败，已重试{max_retry}次")
    # 可以记录到数据库或写入日志文件，以便后续人工处理
    return False
```

---

## 十、数据存储设计

### 11.1 笔记数据表（Excel Sheet1）

| 字段 | 说明 |
|------|------|
| 笔记ID | note_id |
| 笔记链接 | note_url |
| 笔记类型 | 图集/视频 |
| 用户ID | user_id |
| 昵称 | nickname |
| 标题 | title |
| 描述 | desc |
| 点赞数 | liked_count |
| 收藏数 | collected_count |
| 评论数 | comment_count |
| 分享数 | share_count |
| 上传时间 | upload_time |
| IP归属地 | ip_location |
| 采集时间 | crawl_time |

### 11.2 评论数据表（Excel Sheet2）

| 字段 | 说明 |
|------|------|
| 笔记ID | note_id（关联字段）|
| 评论ID | comment_id |
| 父评论ID | parent_comment_id |
| 评论层级 | comment_level |
| 用户ID | user_id |
| 昵称 | nickname |
| 评论内容 | content |
| 点赞数 | like_count |
| 发布时间 | upload_time |
| IP归属地 | ip_location |
| 首次发现时间 | first_seen_time |

---

## 十一、异常处理与限流策略

### 12.1 异常分类处理

| 异常类型 | 处理策略 |
|----------|----------|
| **请求失败**（网络/超时） | 使用 `@retry` 装饰器，3次重试，指数退避 |
| **签名失效**（X-s验证失败） | 检查Cookie有效性，提示重新登录 |
| **频率限制**（429/滑动验证） | 随机延迟 1-3 秒，增加代理池轮换 |
| **数据缺失**（字段为空） | 使用默认值填充，记录日志 |
| **SQLAlchemy连接失败** | 连接池重试，最多5次 |
| **事务执行失败** | 自动回滚，记录错误日志 |

### 12.2 SQLAlchemy连接池配置

```python
from sqlalchemy import create_engine

# 连接池配置
engine = create_engine(
    'mysql+pymysql://user:password@host:port/database?charset=utf8mb4',
    pool_size=5,              # 连接池大小
    max_overflow=10,          # 最大溢出连接数
    pool_recycle=3600,        # 连接回收时间（秒）
    pool_pre_ping=True,       # 连接前ping测试，自动剔除失效连接
    pool_timeout=30,          # 获取连接超时时间
    echo=False                # 是否打印SQL语句（调试用）
)
```

### 12.3 事务管理最佳实践

```python
# 使用上下文管理器确保事务正确提交/回滚
from contextlib import contextmanager

@contextmanager
def get_db_session(db_manager):
    """数据库会话上下文管理器"""
    session = db_manager.get_session()
    try:
        yield session
        session.commit()
    except Exception as e:
        session.rollback()
        raise e
    finally:
        session.close()

# 使用示例
with get_db_session(db_manager) as session:
    # 执行数据库操作
    session.add(new_record)
    # 自动提交或回滚
```

### 12.4 限流策略

```python
import random
import time

def random_delay(min_seconds=1, max_seconds=3):
    """随机延迟，避免触发反爬"""
    time.sleep(random.uniform(min_seconds, max_seconds))

# 每页查询后延迟
random_delay(1, 2)

# 每条笔记评论采集后延迟
random_delay(0.5, 1.5)
```

---

## 十二、风险与应对

| 风险点 | 应对措施 |
|--------|----------|
| **Cookie失效** | **Cookie池机制**：多账号轮换，自动失效检测与切换 |
| **单点故障** | 至少配置3-5个可用Cookie，避免单Cookie失效导致系统停摆 |
| **频率限制** | 多Cookie负载均衡，分散请求压力 |
| **账号封禁** | Cookie池健康检查，自动剔除异常账号 |
| 反爬升级 | 监控请求成功率，及时调整延迟策略 |
| 数据量大 | SQLAlchemy连接池+批量操作优化 |
| 评论数据过多 | 设置单条笔记最大评论数限制 |
| 定时任务重叠 | 使用数据库锁或文件锁防止重复执行 |
| MySQL连接耗尽 | SQLAlchemy连接池自动管理 |
| 存储空间不足 | 定期归档历史数据，分区表管理 |

---

## 十三、部署与运行

### 13.1 环境准备

#### 1. 安装依赖

```bash
pip install sqlalchemy pymysql apscheduler
```

#### 2. 创建数据库并初始化表结构

**⚠️ 重要：运行代码前必须手动执行以下步骤创建数据库和表！**

```bash
# Step 1: 创建数据库
mysql -u root -p -e "CREATE DATABASE xhs_crawler CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# Step 2: 执行初始化脚本创建表（必须）
mysql -u root -p xhs_crawler < init.sql
```

或进入 MySQL 后执行：
```bash
mysql -u root -p
USE xhs_crawler;
source /path/to/init.sql;
```

**init.sql 脚本位置**：参考 [6.3 SQL 初始化脚本](#63-sql-初始化脚本手动执行)

### 13.2 配置文件

```python
# config.py

# 数据库配置（SQLAlchemy格式）
DB_CONFIG = {
    'url': 'mysql+pymysql://root:your_password@localhost:3306/xhs_crawler?charset=utf8mb4',
    'pool_size': 5,
    'max_overflow': 10,
    'pool_recycle': 3600
}

# 爬虫配置
CRAWL_CONFIG = {
    'keyword': '51talk老带新',
    'max_pages': 5,
    'sort_type': 1,
    'page_size': 20,
    'schedule_interval': 3600
}

# Cookie池配置
COOKIE_POOL_CONFIG = {
    'max_fail_count': 5,
    'switch_strategy': 'round_robin'
}

# Cookie列表
COOKIE_POOL = [
    {
        'name': 'account_01',
        'value': 'acw_tc=xxx; a1=xxx; web_session=xxx; ...',
        'account_info': '主账号'
    },
    {
        'name': 'account_02',
        'value': 'acw_tc=yyy; a1=yyy; web_session=yyy; ...',
        'account_info': '备用账号1'
    }
]
```

### 13.3 Cookie池初始化

```python
# init_cookie_pool.py
# 注意：运行前需先手动执行 init.sql 创建数据库表
from config import DB_CONFIG, COOKIE_POOL
from models import DatabaseManager
from cookie_pool_manager import CookiePoolManager

def init_cookie_pool():
    # 确保已手动执行 SQL 脚本创建表
    db_manager = DatabaseManager(DB_CONFIG['url'])
    cookie_manager = CookiePoolManager(db_manager)
    
    for cookie_info in COOKIE_POOL:
        cookie_manager.add_cookie(
            cookie_info['name'],
            cookie_info['value'],
            cookie_info['account_info']
        )
    
    print("Cookie池初始化完成")
    cookie_manager.print_pool_status()

if __name__ == '__main__':
    init_cookie_pool()
```

运行：
```bash
python init_cookie_pool.py
```

### 13.4 启动命令

```bash
# 方式1：直接运行（单次采集）
python crawler_incremental.py

# 方式2：启动定时任务（每小时自动执行）
python scheduler.py

# 方式3：后台运行（Linux）
nohup python scheduler.py > logs/scheduler.log 2>&1 &
```

---

## 十四、参考文档

- 项目架构文档：`ARCHITECTURE.md`
- API接口文档：`apis/xhs_pc_apis.py`
- 数据处理工具：`xhs_utils/data_util.py`
- SQLAlchemy文档：https://docs.sqlalchemy.org/
- APScheduler文档：https://apscheduler.readthedocs.io/
